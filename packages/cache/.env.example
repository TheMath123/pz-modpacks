# This is just an example of how to configure the secondary Redis connection
# when using the `packages/cache` package.
# (do not create any `.env` file inside the `packages/cache` folder):

SECONDARY_DATABASE_URL="redis://localhost:63790"
